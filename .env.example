# Copy this file to .env only if a later tool needs connection settings.
# Never commit the real .env file or a database password.
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=supply_chain_control_tower
PGUSER=your_local_postgres_user
